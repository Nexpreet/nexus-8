Immediate operands are stored as is

Currently, there are no signed interpretations but rather the math wraps it to 8bit

Sign flag will be implemented later on

Currently, arbitrary opcodes are treated as NOP instead of HALT, may change it later 

The cpu has passed all the tests successfully

